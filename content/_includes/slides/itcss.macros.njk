{% import "slides/embed.macros.njk" as embed %}

{% macro main(slide, page) %}
{% set slideBase %}
img: patterns/itcss-metrics.png
alt: |
  inverted triange, with
  range accross the top,
  and specificity/explicitness
  down the sides
fit: contain
background: white;
qr: http://technotif.com/manage-large-css-projects-with-itcss/
caption: |
  [**Inverted Triangle** CSS](http://technotif.com/manage-large-css-projects-with-itcss/)
  by [**Harry Roberts**](https://twitter.com/csswizardry)
{% endset %}
{% set slide_yaml = slideBase | yaml | merge(slide) %}
  {{ embed.img(
    slide=slide_yaml,
    page=page
  ) }}
{% endmacro %}

{% macro layers(slide, page) %}
{% set slideBase %}
img: patterns/itcss-layers.png
alt: |
  Triangle, point down, with layers:
  settings, tools, generics, elements,
  objects, components, overrides
fit: contain
background: white
qr: http://technotif.com/manage-large-css-projects-with-itcss/
caption: |
  [**Inverted Triangle** CSS](http://technotif.com/manage-large-css-projects-with-itcss/)
  by [**Harry Roberts**](https://twitter.com/csswizardry)
{% endset %}
{% set slide_yaml = slideBase | yaml | merge(slide) %}
  {{ embed.img(
    slide=slide_yaml,
    page=page
  ) }}
{% endmacro %}

{% macro un_metrics(slide, page) %}
{% set slideBase %}
img: patterns/un-itcss-metrics.png
alt: |
  Harry Robberts Inverted Triangle,
  but it's been flipped over,
  so now it's just a normal triangle
fit: contain
background: white
caption: |
  ~~Inverted~~ **Normal Triangle**
  by [~~Harry Roberts~~](https://twitter.com/csswizardry) ~~Maslow~~ **Miriam**
{% endset %}
{% set slide_yaml = slideBase | yaml | merge(slide) %}
  {{ embed.img(
    slide=slide_yaml,
    page=page
  ) }}
{% endmacro %}

{% macro un_layers(slide, page) %}
{% set slideBase %}
img: css-next/un-itcss-layers.png
alt: |
  triangle
  with labeled layers from
  wide base to narrow point —
  settings, tools, generic,
  elements, objects, components,
  overrides
fit: contain
background: white
caption: |
  [**Inverted Triangle**](http://technotif.com/manage-large-css-projects-with-itcss/)
  CSS (ITCSS), by Harry Roberts
{% endset %}
{% set slide_yaml = slideBase | yaml | merge(slide) %}
  {{ embed.img(
    slide=slide_yaml,
    page=page
  ) }}
{% endmacro %}

{% macro harry(slide, page) %}
{% set slideBase %}
face: harryroberts.jpg
title: |
  **Harry** Roberts
sub: |
  [**Inverted Triangle**](http://technotif.com/manage-large-css-projects-with-itcss/)
  CSS (ITCSS)
{% endset %}
{% set slide_yaml = slideBase | yaml | merge(slide) %}
  {{ embed.person(
    slide=slide_yaml,
    page=page
  ) }}
{% endmacro %}

{% macro graph(slide, page) %}
{% set slideBase %}
img: layers/chart-itcss.jpg
alt: |
  A graph
  with specificity on the vertical axis,
  and code line numbers horizontal,
  divided into layers,
  and a line showing that
  specificity should only increase
  throughout the code base
fit: contain
background: white
caption: |
  [**Inverted Triangle**](http://technotif.com/manage-large-css-projects-with-itcss/)
  CSS (ITCSS)
{% endset %}
{% set slide_yaml = slideBase | yaml | merge(slide) %}
  {{ embed.img(
    slide=slide_yaml,
    page=page
  ) }}
{% endmacro %}
